# Contributing to Receipt Parser Yomi Bot

## Благодарим за интерес к проекту!

Мы приветствуем вклад от сообщества. Вот как вы можете помочь улучшить проект.

## Содержание
- [Code of Conduct](#code-of-conduct)
- [Как начать](#как-начать)
- [Процесс разработки](#процесс-разработки)
- [Стиль кодирования](#стиль-кодирования)
- [Отправка изменений](#отправка-изменений)
- [Отчет об ошибках](#отчет-об-ошибках)
- [Запрос новых функций](#запрос-новых-функций)

## Code of Conduct

Этот проект придерживается профессиональных стандартов поведения. Участвуя в проекте, вы соглашаетесь:
- Быть уважительными к другим участникам
- Принимать конструктивную критику
- Фокусироваться на том, что лучше для проекта
- Проявлять эмпатию к другим участникам сообщества

## Как начать

### 1. Форк репозитория

```bash
# Форкните репозиторий через GitHub UI
# Затем клонируйте свой форк
git clone https://github.com/YOUR_USERNAME/receipt_parser_yomi_bot.git
cd receipt_parser_yomi_bot
```

### 2. Настройте окружение

```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate для Windows

# Установите зависимости
pip install -r requirements.txt

# Установите системные библиотеки (Ubuntu/Debian)
sudo apt-get install -y libzbar0 tesseract-ocr tesseract-ocr-rus poppler-utils
```

### 3. Настройте конфигурацию

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Заполните .env своими тестовыми ключами
# НЕ используйте production ключи для разработки
```

### 4. Создайте ветку

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

## Процесс разработки

### Структура проекта

```
receipt_parser_yomi_bot/
├── bot.py                 # Telegram bot interface
├── receipt_processor.py   # Receipt processing orchestration
├── openai_vision.py      # OpenAI Vision API integration
├── qr_parser.py          # QR code parsing
├── ocr_handler.py        # OCR fallback
├── drive_handler.py      # Google Drive operations
├── sheets_handler.py     # Google Sheets operations
├── google_auth.py        # Google OAuth
├── requirements.txt      # Python dependencies
├── .env.example          # Environment variables template
├── README.md            # Project overview
├── ARCHITECTURE.md      # System architecture
├── API.md              # API reference
├── USAGE.md            # Usage guide
└── CONTRIBUTING.md     # This file
```

### Запуск бота для разработки

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите бота
python3 bot.py
```

### Проверка изменений

```bash
# Форматирование кода
black *.py

# Проверка стиля (если установлен flake8)
flake8 *.py

# Проверка типов (если установлен mypy)
mypy *.py
```

## Стиль кодирования

### Python Code Style

Мы следуем [PEP 8](https://www.python.org/dev/peps/pep-0008/) с некоторыми исключениями:

- **Длина строки**: 100 символов (не 79)
- **Импорты**: группируйте в порядке: стандартная библиотека → сторонние → локальные
- **Docstrings**: используйте для всех публичных функций и классов
- **Type hints**: желательны для новых функций

### Пример хорошего кода

```python
from typing import Tuple, Dict, Optional
import os

def process_receipt(image_path: str, validate: bool = True) -> Tuple[bool, Dict, str]:
    """
    Обработка чека из изображения.
    
    Args:
        image_path: Путь к файлу изображения
        validate: Выполнять ли валидацию данных
    
    Returns:
        Кортеж (success, data, message)
        - success: True если обработка успешна
        - data: Словарь с данными чека
        - message: Сообщение об ошибке или "OK"
    
    Raises:
        FileNotFoundError: Если файл не существует
    """
    if not os.path.exists(image_path):
        raise FileNotFoundError(f"File not found: {image_path}")
    
    # Ваш код здесь
    return True, {}, "OK"
```

### Комментарии

- Пишите комментарии на русском языке (как и остальной код)
- Объясняйте "почему", а не "что"
- Используйте docstrings для документирования API

```python
# Плохо
# Открываем файл
file = open(path)

# Хорошо
# Используем UTF-8 кодировку для корректной работы с русскими именами
file = open(path, encoding='utf-8')
```

### Обработка ошибок

```python
# Хорошо: специфичные исключения
try:
    data = process_receipt(image_path)
except FileNotFoundError:
    logger.error(f"Receipt file not found: {image_path}")
    return False, {}, "Файл не найден"
except Exception as e:
    logger.error(f"Unexpected error: {e}")
    return False, {}, f"Ошибка обработки: {str(e)}"
```

### Асинхронный код

Для Telegram bot handlers используйте async/await:

```python
async def handle_photo(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик фотографий."""
    await update.message.reply_text("Обрабатываю...")
    # Ваш код
```

## Отправка изменений

### 1. Убедитесь в качестве кода

```bash
# Проверьте, что код работает
python3 bot.py

# Отправьте тестовое фото в бота
# Проверьте логи на ошибки
```

### 2. Коммитьте изменения

```bash
git add .
git commit -m "Add: описание изменения"
```

**Формат commit messages:**
- `Add: новая функциональность`
- `Fix: исправление бага`
- `Update: обновление существующего кода`
- `Refactor: рефакторинг без изменения функциональности`
- `Docs: обновление документации`
- `Style: форматирование кода`

**Хорошие примеры:**
```
Add: support for JPEG receipts
Fix: QR code detection on low-quality images
Update: OpenAI API to latest version
Docs: add troubleshooting section to USAGE.md
```

### 3. Отправьте в свой форк

```bash
git push origin feature/your-feature-name
```

### 4. Создайте Pull Request

1. Перейдите на GitHub
2. Нажмите "New Pull Request"
3. Выберите свою ветку
4. Заполните шаблон:

```markdown
## Описание

Кратко опишите изменения.

## Тип изменения

- [ ] Bug fix (исправление бага)
- [ ] New feature (новая функциональность)
- [ ] Breaking change (изменение, ломающее обратную совместимость)
- [ ] Documentation update (обновление документации)

## Тестирование

Опишите, как вы тестировали изменения:
- [ ] Протестировал локально
- [ ] Отправил тестовые чеки
- [ ] Проверил загрузку на Drive
- [ ] Проверил добавление в Sheets

## Скриншоты (если применимо)

Добавьте скриншоты для UI изменений.

## Checklist

- [ ] Код следует стилю проекта
- [ ] Добавлены комментарии для сложной логики
- [ ] Обновлена документация (если нужно)
- [ ] Изменения не ломают существующую функциональность
```

## Отчет об ошибках

### Перед созданием issue

1. Проверьте, что ошибка воспроизводится на последней версии
2. Поищите похожие issues (открытые и закрытые)
3. Соберите информацию об окружении

### Создание Bug Report

Создайте issue со следующей информацией:

```markdown
**Описание бага**
Четкое описание проблемы.

**Как воспроизвести**
1. Шаг 1
2. Шаг 2
3. Шаг 3

**Ожидаемое поведение**
Что должно происходить.

**Актуальное поведение**
Что происходит на самом деле.

**Скриншоты**
Если применимо.

**Окружение:**
- OS: [например, Ubuntu 22.04]
- Python версия: [например, 3.12.0]
- Версия бота: [commit hash или tag]

**Логи**
```
Вставьте релевантные логи
```

**Дополнительный контекст**
Любая другая информация о проблеме.
```

## Запрос новых функций

### Перед созданием feature request

1. Проверьте, что функция еще не запрошена
2. Подумайте, как функция вписывается в общую архитектуру
3. Рассмотрите альтернативные подходы

### Создание Feature Request

```markdown
**Проблема, которую решает функция**
Четкое описание проблемы. Например: "Сложно найти чеки за определенный период..."

**Предлагаемое решение**
Описание желаемой функциональности.

**Альтернативы**
Другие способы решения проблемы.

**Дополнительный контекст**
Примеры использования, мокапы, etc.
```

## Области для улучшения

Вот некоторые области, где проект нуждается в помощи:

### Высокий приоритет

- [ ] Добавить unit тесты
- [ ] Добавить integration тесты
- [ ] Улучшить обработку ошибок
- [ ] Добавить retry механизм для API вызовов
- [ ] Добавить метрики и мониторинг

### Средний приоритет

- [ ] Поддержка редактирования данных через inline клавиатуру
- [ ] Экспорт данных в различных форматах
- [ ] Статистика и аналитика по чекам
- [ ] Поддержка нескольких языков
- [ ] Docker контейнеризация

### Низкий приоритет

- [ ] Web интерфейс для управления
- [ ] Поддержка других мессенджеров (WhatsApp, etc.)
- [ ] Машинное обучение для улучшения распознавания
- [ ] Интеграция с бухгалтерскими системами

## Вопросы?

Если у вас есть вопросы о процессе разработки:

1. Проверьте [USAGE.md](USAGE.md) для общих вопросов
2. Проверьте [ARCHITECTURE.md](ARCHITECTURE.md) для технических вопросов
3. Проверьте [API.md](API.md) для вопросов об API
4. Создайте issue с меткой "question"

## Лицензия

Внося изменения в этот проект, вы соглашаетесь, что ваш вклад будет лицензирован под MIT License.

## Благодарности

Спасибо всем контрибьюторам, которые помогают улучшить этот проект! 🎉
